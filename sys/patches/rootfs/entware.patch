Add entware paths to profile
Index: hyperdiver/rootfs/etc/profile
===================================================================
--- hyperdiver.orig/rootfs/etc/profile
+++ hyperdiver/rootfs/etc/profile
@@ -10,6 +10,10 @@ set_path_before()
 PATH="/usr/bin:/usr/sbin:/bin:/sbin:/mnt/bin"
 set_path_before /usr/local/sbin
 set_path_before /usr/local/bin
+set_path_before /opt/bin
+set_path_before /opt/sbin
+set_path_before /opt/local/bin
+set_path_before /opt/local/sbin
 
 LD_LIBRARY_PATH="/usr/local/lib:/usr/lib:/mnt/lib"
 
@@ -17,7 +21,8 @@ export PATH
 export LD_LIBRARY_PATH
 
 # ANSI COLORS
-CRE="[K"
+CRE="
+[K"
 NORMAL="[0;39m"
 RED="[1;31m"
 GREEN="[1;32m"
